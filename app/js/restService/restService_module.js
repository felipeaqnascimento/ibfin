'use strict';

/* Module for RestService */

var restServiceModule = angular.module('restService.module', ['myApp']);

/**
 * Module for ordensServicos
 */
restServiceModule.config(['$routeProvider', function($routeProvider) {
    
}]);
